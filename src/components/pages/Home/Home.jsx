import GameSmashPass from '../../GameSmashPass/GameSmashPass';

const Home = ({ divRef }) => {
	return (
		<main className='main'>
			<GameSmashPass divRef={divRef} />
		</main>
	);
};

export default Home;
